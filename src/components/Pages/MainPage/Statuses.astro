<!-- Status & Errors -->
<section
  id="status"
  class="mx-auto max-w-6xl px-4 sm:px-6 py-14"
>
  <h2 class="text-2xl font-bold">Status & Errors</h2>
  <div class="mt-4 grid md:grid-cols-3 gap-4">
    <div class="rounded-lg border border-gray-200 bg-white p-4">
      <div class="font-semibold">200 OK</div>
      <p class="mt-1 text-sm text-gray-600">
        JSON payload with recommended settings.
      </p>
    </div>
    <div class="rounded-lg border border-gray-200 bg-white p-4">
      <div class="font-semibold">400 Bad Request</div>
      <p class="mt-1 text-sm text-gray-600">
        Missing/invalid parameters. Returns <span class="font-mono"
          >&lcub; "error": "message" &rcub;</span
        >.
      </p>
    </div>
    <div class="rounded-lg border border-gray-200 bg-white p-4">
      <div class="font-semibold">500 Server Error</div>
      <p class="mt-1 text-sm text-gray-600">
        Unexpected failure; retry or report.
      </p>
    </div>
  </div>

  <div class="mt-6 rounded-xl border border-gray-200 bg-white p-4">
    <div class="font-semibold">Versioning & Stability</div>
    <ul class="mt-2 list-disc pl-5 text-sm text-gray-700 space-y-1">
      <li>
        Current ruleset: <span class="font-mono">v1</span>. Pass <span
          class="font-mono">version</span
        > to pin behavior.
      </li>
      <li>
        Idempotent GETs. No rate limits enforced by default (set at deploy).
      </li>
      <li>
        Outputs aim to be conservative and production-safe; always benchmark for
        your workload.
      </li>
    </ul>
  </div>
</section>
